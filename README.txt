# HansAirlineTickets
Project for ASP.net

Steps to document changes:
1) Go to Documents folder
2) Right click on "HansAirlines" and click "Git bash here"
3) Type in the following commands:
  git pull origin master
  git add -A
  git commit -m "Put information about changes here"
  git push origin master
